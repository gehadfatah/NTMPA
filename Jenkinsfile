pipeline {

    agent any

    stages {

         stage('build') {
                    steps {
                     echo 'building the build'
                    }
          }
           stage('test') {
                      steps {
                       echo 'building the test'
                      }
                  }
         stage('deploy') {
                   steps {
                       echo 'building the deploy'
                  }
         }

    }

}
